VITE_APP_ENV="development"
VITE_API_BASE_URL="http://127.0.0.1:8000"
VITE_API_TIMEOUT="0"

# Copy this file to .env.development, .env.staging, or .env.production
# and replace the API URL for each environment as needed.
# Do not store secrets or credentials in frontend environment files.
